<!-- changelog: none -->

This PR updates repository URLs after the GitHub rename from
zakura-core/libraries to zakura-core/common. The only `Cargo.toml` change is
the `workspace.package.repository` metadata URL; no crate API or behavior
changes.
